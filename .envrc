# Automatically use nix flake for development environment
use flake

# Watch additional files that should trigger reload
watch_file flake.nix
watch_file package.json

# Layout bun-specific settings (optional)
layout node

# Set development environment variables
export OPENCODE_DEV=1
export NODE_ENV=development

# Inform the developer
echo "✅ OpenCode plugin development environment loaded"
